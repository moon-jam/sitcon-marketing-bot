services:
  bot:
    build: .
    container_name: sitcon-review-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Taipei
      - DB_PATH=/app/data/reviews.db
